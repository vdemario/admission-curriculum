{
  "name": "admission-curriculum",
  "version": "1.0.0",
  "description": "Quizzes and exercises of the admission process @Laboratoria",
  "scripts": {
    "prebuild": "node index.js",
    "compile": "curriculum-parser topic ./build/spanish --version 1.0.0 --locale es-ES --track js --repo Laboratoria/admission-curriculum > ./build/spanish.json",
    "build:dev": "yarn prebuild --env=dev & yarn parse",
    "build:staging": "npm run prebuild --env=staging && npm run parse",
    "build:production": "npm run prebuild --env=production && npm run parse",
    "mdlint": "mdlint .",
    "validate": "course-parser spanish/README.md  --track=js --locale=es-ES --validate",
    "pretest": "npm run mdlint && npm run validate",
    "test": "mocha './{,!(node_modules|build)/**/}*.spec.js'"
  },
  "repository": "Laboratoria/admission-curriculum",
  "keywords": [
    "javascript",
    "laboratoria",
    "admission"
  ],
  "author": "Laboratoria <sumate@laboratoria.la> (http://laboratoria.la/)",
  "license": "CC-BY-SA-4.0",
  "bugs": {
    "url": "https://github.com/Laboratoria/admission-curriculum/issues"
  },
  "dependencies": {
    "curriculum-parser": "Laboratoria/curriculum-parser",
    "fs-extra": "^7.0.1",
    "minimist": "^1.2.0",
    "replace-in-file": "^4.0.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "course-parser": "Laboratoria/course-parser#1.1.1",
    "mdlint": "Laboratoria/mdlint#v1.0.0",
    "mocha": "^5.2.0"
  }
}
